name: Shell Commands 

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Hello World"
      - name: multiline script 
        run: |
           node -v 
           npm -v
  mustafa-job1:
    runs-on: ubuntu-latest
    steps:
      - name: echo n stuff
        run: echo "There We go!"